"""Lesson 04: user input. Uncomment the last lines to try it."""


def area_m2(length_m, width_m):
    return length_m * width_m


print(f"Default area: {area_m2(2.5, 1.2):.2f} m²")

# length_m = float(input("Length in m: "))
# width_m = float(input("Width in m: "))
# print(f"Area: {area_m2(length_m, width_m):.2f} m²")
